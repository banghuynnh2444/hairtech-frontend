# Copy to .env.local for development. The committed .env.production contains the public production API URL.
VITE_API_URL=http://localhost:3000
# Chỉ dùng cho npm run dev trong trình duyệt; không đặt trong .env.production.
VITE_DEV_DEVICE_FINGERPRINT=
